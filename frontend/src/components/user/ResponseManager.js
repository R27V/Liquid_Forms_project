import React from 'react'

const ResponseManager = ({responses}) => {
  
    const showResponses = () => {
        return responses
    }
  
    return (
    <div>ResponseManager</div>
  )
}

export default ResponseManager